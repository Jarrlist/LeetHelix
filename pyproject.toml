[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "leet-helix"
version = "0.2.1"
description = "Practice editing in Helix"
authors = [{name = "User"}]
dependencies = [
    "typer",
    "rich",
    "sqlmodel",
    "sqlite-utils",
    "pytest",
    "pyntercept==0.0.21",
]

[project.scripts]
leet = "leet_helix.main:app"
